# Private Sample Staging

This directory is reserved for local-only notes about private samples. Files in this folder are ignored by git except this README.

Place raw portfolio samples in `private-samples-raw/`, then run:

```bash
npm run encrypt
```

Only encrypted files from `samples/encrypted/` should be deployed.
